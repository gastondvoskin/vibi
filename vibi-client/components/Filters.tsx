"use client";
import SearchBar from "./SearchBar";
import SelectFilter from "./SelectFilter";

const Filters = () => {
  return (
    <div>
      <SearchBar />
      <SelectFilter />
    </div>
  );
};

export default Filters;
